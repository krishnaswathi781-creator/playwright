// import {test, expect} from '
const base
await PageRevealEvent.goto(baseURL)
//Radio Button -Male and female
await pageXOffset.check("#female")
await expectpage.locator("#female")

await expect(pageXOffset.locator("#male").toBeChecked());

for(let day of days{
    await pageXOffset.check(day);
    await expect(pageXOffset.locator(day)).toBechecked();
}
let uncheckdays = ["thursday", "#friday", "#saturday"]
for{let uncheckday}